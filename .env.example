# Copy this file to `.env` (Vite does not load `.env.example`), then restart `npm run dev`.
# Base API URL — the app sets `deviceid` per sidebar tab (123 = MK3 Diesel/FCJC, 124 = MK3 Electric/FTJP)
VITE_PACKET_API_URL=https://7hlj67lc34.execute-api.us-east-1.amazonaws.com/Production/latest?deviceid=123
